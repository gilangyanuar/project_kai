name: cp_kaichecksheet
description: "A new Flutter project."

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.1
  google_fonts: ^6.3.2
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  url_launcher: ^6.2.1

  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/logo_warna.png
    - assets/logo_putih.png
